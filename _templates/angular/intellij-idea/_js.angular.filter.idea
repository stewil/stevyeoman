/**
 * Created by ${USER} on ${DATE}.
 */
(function(){
    "use strict";

    angular.module('${NAME}.module', [])
        .filter('${NAME}Filter');

    ${NAME}Filter.${DS}inject = [];

    function ${NAME}Filter(){
        return function(input, args){
            return true;
        }
    }
})();