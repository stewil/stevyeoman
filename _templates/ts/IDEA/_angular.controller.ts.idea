/**
 * Created by ${USER} on ${DATE}.
 */
module ${PROJECT_NAME}.${NAME}.controllers {
    "use strict";

    export class ${NAME}Controller{
        static ${DS}inject} = ["${DS}scope"];
        constructor(private ${DS}scope : angular.IScope){

        }
        exampleFunc = (arg?:any) =>{
        }
    }
}