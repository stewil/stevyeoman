/**
 * Created by ${USER} on ${DATE}.
 */
module ${PROJECT_NAME}.${NAME}.services {
    "use strict";

    export interface I${NAME}Service{
        exampleFunc(arg:any):void
    }

    export class AngularService implements IAngularService{

    public static ${DS}inject} = ["${DS}rootScope"];

        constructor(private ${DS}rootScope:angular.IRootScopeService){
        }

        exampleFunc = (arg:any) =>{
        };
    }
}